apiVersion: v1
kind: Service
metadata:
  name: backend-api
  labels:
    app: dotnet-backend
spec:
  type: ClusterIP
  selector:
    app: dotnet-backend
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
